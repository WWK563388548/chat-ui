import styled from 'styled-components';
import { MessageCardContainer } from '../MessageCard/style';

export const StyledMessageList = styled.div`
  ${MessageCardContainer} {
    margin-bottom: 20px;
  }
`;

export const MessageListContainer = styled.div``;